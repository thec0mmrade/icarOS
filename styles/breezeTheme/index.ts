import { type DefaultTheme } from "styled-components";
import colors from "styles/breezeTheme/colors";
import formats from "styles/breezeTheme/formats";
import sizes from "styles/breezeTheme/sizes";

const breezeTheme: DefaultTheme = {
  colors,
  formats,
  name: "Breeze",
  sizes,
};

export default breezeTheme;
